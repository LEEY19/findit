$('#video-list').append('<%= j render @videos %>');
<% if @videos.next_page %>
  window.canScroll = true
  $('.pagination').replaceWith('<%= j paginate @videos %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
