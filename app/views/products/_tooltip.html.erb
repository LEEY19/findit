<img src="<%= product.picture %>" alt="Haha" class="product_picture">
<h4><%= product.title %></h4>
<div class="actions">
  <%= link_to "Buy Now", product.product_link, class: 'btn', target: '_blank' %>
  <%= link_to "Want an exact match?", exact_match_product_path(product), class: 'btn', remote: true %>

</div>
